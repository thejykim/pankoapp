import { FocusInputDirective } from './focus-input.directive';

describe('FocusInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusInputDirective();
    expect(directive).toBeTruthy();
  });
});
