import { PieChartZeroPaddingDirective } from './pie-chart-zero-padding.directive';

describe('PieChartZeroPaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new PieChartZeroPaddingDirective();
    expect(directive).toBeTruthy();
  });
});
